import 'package:flutter/material.dart';

class DiaryEditPage extends StatelessWidget {
  final String? date;

  const DiaryEditPage({super.key, this.date});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Diary Edit Page — 占位'),
    );
  }
}
