import 'package:flutter/material.dart';

class DayCounterAddPage extends StatelessWidget {
  const DayCounterAddPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Day Counter Add Page — 占位'),
    );
  }
}
