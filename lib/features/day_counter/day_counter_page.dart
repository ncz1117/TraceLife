import 'package:flutter/material.dart';

class DayCounterPage extends StatelessWidget {
  const DayCounterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Day Counter Page — 占位'),
    );
  }
}
